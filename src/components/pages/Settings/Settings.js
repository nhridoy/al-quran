import React from "react";
import { Header } from "../../Header/Header";

export const Settings = () => {
  return (
    <div>
      <Header head="Settings" />
      <h1>Settings</h1>
    </div>
  );
};
